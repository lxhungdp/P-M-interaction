# Archive — not used at runtime

Legacy files kept for reference only:

- `app_v6_monolith.py` — original ~1900-line `app.py` before refactor
- `patch_*.py`, `fix_*.py` — one-off AI edit scripts (deprecated)

Do not import from this folder. See `AGENTS.md` and `.cursor/rules/no-patch-scripts.mdc`.
